# Loaded automatically when NODE_ENV=development (the default for `npm run dev`).
# Copy to .env.development and adjust as needed. Real file is gitignored.
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/fitness_tracking_dev
JWT_SECRET=dev_jwt_secret_change_me
JWT_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=12
